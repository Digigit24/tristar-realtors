! function(p) {
    "use strict";

    function i(t, o, n) {
        var s = n.offset().top,
            r = n.outerHeight(),
            i = n.width(),
            e = o[0].scrollWidth,
            l = o.height(),
            a = parseInt(o.css("top"), 10),
            c = o.offset().top,
            d = e - i,
            e = t.scrollTop(),
            i = o.data("relative");
        i && (t = p(i).outerHeight(), h = c + l - (s + r), s <= c && h <= -2 ? p(i).css({
            position: "sticky",
            top: -(t - a),
            transform: "none"
        }) : -2 <= h ? p(i).css({
            transition: "none",
            transform: `translateY(-${e+a-c}px)`
        }) : p(i).css({
            position: "",
            top: "",
            transform: "none"
        }));
        var h = p("body").hasClass("rtl");
        s <= c && c + l < s + r ? (i = Math.min((e - s + a) / (r - l) * d, d), o.css("transform", `translateX(${h?"":"-"}${i}px)`)) : c + l === s + r && o.css("transform", `translateX(${h?"":"-"}${d}px)`);
        const f = n.find(".horizontal-scroll-line .line-active");
        f && f.length && (l = Math.min(Math.max((e - s + a) / (r - l), 0), 1), f.css("width", `${100*l}%`))
    }
    p(window).on("elementor/frontend/init", () => {
        elementorFrontend.hooks.addAction("frontend/element_ready/spaciaz-horizontal-scroll.default", t => {
            const o = p(window),
                n = t.find(".horizontal-scroll-inner"),
                s = t.find(".horizontal-scroll-line");
            s && s.css({
                width: n[0].scrollWidth
            });
            const r = t.find(".horizontal-scroll-wrapper");
            i(o, n, r), o.on("scroll", () => i(o, n, r)), o.on("resize", () => i(o, n, r))
        })
    })
}(jQuery);